{
  "name": "@testnumbers/number-generator",
  "private": true,
  "version": "0.0.1",
  "main": "lib/index",
  "typings": "lib/index",
  "scripts": {
    "dev": "yarn build && node ./lib/index.js",
    "build": "mkdir -p lib && cp -r ./src/* ./lib",
    "clean": "rm -rf ./lib"
  },
  "dependencies": {
    "express": "^4.16.4",
    "socket.io": "^2.2.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "cross-env": "^5.1.4"
  }
}
